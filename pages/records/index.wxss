.records-shell {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  box-sizing: border-box;
  padding-top: 88rpx;
}

.records-scroll {
  flex: 1;
  height: 100%;
}

.records-list {
  padding-top: 16rpx;
}

.records-item {
  scroll-margin-top: 140rpx;
}

.records-list-bottom-pad {
  height: calc(60rpx + env(safe-area-inset-bottom));
}

.records-empty {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 120rpx 48rpx;
  text-align: center;
}

.records-empty-title {
  font-size: var(--font-lg, 30rpx);
  color: var(--color-text-primary, #2d2a1f);
  font-weight: 500;
}

.records-empty-subtitle {
  margin-top: 16rpx;
  font-size: var(--font-md, 28rpx);
  color: var(--color-text-secondary, #8a8578);
}
