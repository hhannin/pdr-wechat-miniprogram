.page-shell {
  min-height: 100vh;
  padding: 18rpx 28rpx 224rpx;
  box-sizing: border-box;
  background:
    radial-gradient(circle at top right, rgba(31, 106, 70, 0.12), transparent 28%),
    radial-gradient(circle at top left, rgba(199, 141, 63, 0.18), transparent 32%),
    linear-gradient(180deg, #fbf7f1 0%, #f4efe5 62%, #efe8da 100%);
  color: #223c2b;
}

.empty-card,
.record-card,
.loading-card {
  position: relative;
  overflow: hidden;
  background: rgba(255, 251, 245, 0.92);
  border: 1px solid rgba(34, 60, 43, 0.1);
  border-radius: 32rpx;
  box-shadow: 0 22rpx 54rpx rgba(49, 42, 27, 0.1);
}

.empty-desc,
.record-subtitle,
.record-time {
  font-size: 25rpx;
  line-height: 1.7;
  color: rgba(34, 60, 43, 0.68);
}

.empty-card {
  margin-top: 18rpx;
}

.empty-title {
  font-size: 34rpx;
  font-weight: 700;
  color: #223c2b;
}

.records-list {
  margin-top: 12rpx;
  display: flex;
  flex-direction: column;
  gap: 26rpx;
}

.records-pinned-shell {
  position: sticky;
  top: 0;
  z-index: 8;
  display: flex;
  flex-direction: column;
  gap: 0;
  padding: 18rpx 22rpx 20rpx;
  border-radius: 32rpx;
  border: 1px solid rgba(34, 60, 43, 0.08);
  box-shadow: 0 20rpx 48rpx rgba(49, 42, 27, 0.1);
  background: rgba(255, 251, 245, 0.94);
  backdrop-filter: blur(16rpx);
  box-sizing: border-box;
}

.records-pinned-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
  padding: 4rpx 2rpx;
}

.records-pinned-copy {
  display: flex;
  align-items: center;
  gap: 12rpx;
  min-width: 0;
}

.records-pinned-title {
  font-size: 28rpx;
  font-weight: 700;
  color: #223c2b;
}

.records-pinned-count,
.records-pinned-toggle {
  font-size: 24rpx;
  line-height: 1.4;
  color: rgba(34, 60, 43, 0.62);
}

.records-pinned-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  margin-top: 18rpx;
  padding-top: 18rpx;
  border-top: 1px solid rgba(34, 60, 43, 0.06);
}

.records-reminder-shell {
  display: flex;
  flex-direction: column;
  gap: 0;
  padding: 18rpx 22rpx 20rpx;
  border-radius: 32rpx;
  border: 1px solid rgba(34, 60, 43, 0.08);
  box-shadow: 0 20rpx 48rpx rgba(49, 42, 27, 0.1);
  background: rgba(255, 251, 245, 0.94);
  box-sizing: border-box;
}

.records-reminder-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
  padding: 4rpx 2rpx;
}

.records-reminder-copy {
  display: flex;
  align-items: center;
  gap: 12rpx;
  min-width: 0;
}

.records-reminder-title {
  font-size: 28rpx;
  font-weight: 700;
  color: #223c2b;
}

.records-reminder-count,
.records-reminder-toggle {
  font-size: 24rpx;
  line-height: 1.4;
  color: rgba(34, 60, 43, 0.62);
}

.records-reminder-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  margin-top: 18rpx;
  padding-top: 18rpx;
  border-top: 1px solid rgba(34, 60, 43, 0.06);
}

.records-group {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.records-group-title {
  position: sticky;
  top: 0;
  z-index: 3;
  padding: 0 6rpx;
  margin: 0 -4rpx;
  background: linear-gradient(180deg, rgba(244, 239, 229, 0.96), rgba(244, 239, 229, 0.82));
  font-size: 28rpx;
  font-weight: 700;
  color: rgba(34, 60, 43, 0.82);
}

.record-row {
  position: relative;
  overflow: hidden;
  border-radius: 32rpx;
  min-height: 204rpx;
}

.record-actions {
  position: absolute;
  top: 0;
  right: 0;
  bottom: 0;
  width: 360rpx;
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  z-index: 1;
  min-height: 204rpx;
}

.record-action {
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  font-weight: 700;
  color: #ffffff;
  opacity: 0;
  pointer-events: none;
  transition: opacity 180ms ease;
}

.record-action-visible {
  opacity: 1;
  pointer-events: auto;
}

.record-action-edit {
  background: linear-gradient(180deg, #2f7753, #245f42);
}

.record-action-pin {
  background: linear-gradient(180deg, #c68a33, #a66d1f);
}

.record-action-delete {
  background: linear-gradient(180deg, #a44c56, #7f3640);
}

.record-card {
  display: block;
  position: relative;
  z-index: 2;
  padding: 22rpx;
  min-height: 204rpx;
  box-sizing: border-box;
  transition: transform 180ms ease;
}

.record-card-open {
  transform: translateX(-360rpx);
}

.record-card-highlighted {
  border-color: rgba(31, 106, 70, 0.26);
  box-shadow:
    0 22rpx 54rpx rgba(49, 42, 27, 0.1),
    0 0 0 2rpx rgba(31, 106, 70, 0.08);
}

.record-card-pressed {
  opacity: 0.96;
}

.record-card-layout {
  display: flex;
  align-items: stretch;
  gap: 20rpx;
  min-height: 160rpx;
}

.record-thumb {
  flex: 0 0 160rpx;
  width: 160rpx;
  min-height: 160rpx;
  border-radius: 24rpx;
  overflow: hidden;
  background: rgba(244, 239, 229, 0.82);
  border: 1px solid rgba(34, 60, 43, 0.08);
}

.record-photo,
.record-photo-empty {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
}

.record-photo {
  display: block;
}

.record-photo-empty {
  color: rgba(34, 60, 43, 0.44);
  font-size: 24rpx;
  letter-spacing: 2rpx;
}

.record-body {
  flex: 1;
  min-width: 0;
  min-height: 160rpx;
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
}

.record-title {
  font-size: 30rpx;
  line-height: 40rpx;
  min-height: 80rpx;
  font-weight: 700;
  color: #223c2b;
  display: -webkit-box;
  overflow: hidden;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
}

.record-subtitle {
  margin-top: 4rpx;
  font-size: 24rpx;
  line-height: 34rpx;
  display: -webkit-box;
  overflow: hidden;
  -webkit-line-clamp: 1;
  -webkit-box-orient: vertical;
}

.record-time {
  margin-top: 10rpx;
  font-size: 24rpx;
  line-height: 34rpx;
  white-space: nowrap;
}

.record-meta {
  margin-top: 10rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
}

.record-meta-tags {
  display: inline-flex;
  align-items: center;
  justify-content: flex-end;
  gap: 10rpx;
  flex-wrap: wrap;
}

.record-meta .record-time {
  margin-top: 0;
  flex: 1;
  min-width: 0;
}

.record-scene {
  flex: 0 0 auto;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 6rpx 14rpx;
  border-radius: 999rpx;
  background: rgba(31, 106, 70, 0.1);
  color: #1f6a46;
  font-size: 22rpx;
  line-height: 1.2;
  white-space: nowrap;
}

.record-reminder-tag {
  flex: 0 0 auto;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 6rpx 14rpx;
  border-radius: 999rpx;
  font-size: 22rpx;
  line-height: 1.2;
  white-space: nowrap;
}

.record-reminder-tag-active {
  background: rgba(31, 106, 70, 0.12);
  color: #1f6a46;
}

.record-reminder-tag-inactive {
  background: rgba(34, 60, 43, 0.1);
  color: rgba(34, 60, 43, 0.62);
}

.empty-card {
  padding: 34rpx 28rpx 30rpx;
}

.loading-card {
  margin-top: 24rpx;
  padding: 28rpx 26rpx;
}

.loading-title {
  font-size: 34rpx;
  font-weight: 700;
  color: #223c2b;
}

.empty-figure {
  position: relative;
  height: 240rpx;
}

.empty-circle {
  position: absolute;
  border-radius: 999rpx;
}

.empty-circle-large {
  width: 220rpx;
  height: 220rpx;
  right: 20rpx;
  top: 0;
  background: radial-gradient(circle, rgba(199, 141, 63, 0.22), transparent 68%);
}

.empty-circle-small {
  width: 140rpx;
  height: 140rpx;
  left: 10rpx;
  bottom: 14rpx;
  background: radial-gradient(circle, rgba(31, 106, 70, 0.18), transparent 68%);
}

.empty-card-shape {
  position: absolute;
  left: 60rpx;
  right: 70rpx;
  top: 34rpx;
  bottom: 26rpx;
  border-radius: 28rpx;
  background:
    linear-gradient(180deg, rgba(255, 255, 255, 0.95), rgba(246, 239, 227, 0.88));
  border: 1px solid rgba(34, 60, 43, 0.08);
  box-shadow: 0 18rpx 40rpx rgba(44, 38, 27, 0.08);
}

.empty-desc {
  margin-top: 14rpx;
}

.empty-action {
  display: inline-flex;
  margin-top: 24rpx;
  align-items: center;
  justify-content: center;
  padding: 18rpx 28rpx;
  border-radius: 999rpx;
  background: linear-gradient(135deg, #1f6a46, #2b7c55);
  color: #f7fff9;
  font-size: 26rpx;
  font-weight: 600;
}

.footer-nav {
  position: fixed;
  left: 28rpx;
  right: 28rpx;
  bottom: calc(constant(safe-area-inset-bottom) + 28rpx);
  bottom: 28rpx;
  bottom: calc(env(safe-area-inset-bottom) + 28rpx);
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 16rpx;
  padding: 14rpx;
  border-radius: 28rpx;
  background: rgba(255, 252, 247, 0.92);
  border: 1px solid rgba(34, 60, 43, 0.08);
  box-shadow: 0 20rpx 56rpx rgba(44, 38, 27, 0.16);
  z-index: 16;
}

.footer-nav-item {
  display: block;
  padding: 20rpx 16rpx;
  border-radius: 22rpx;
  background: transparent;
  text-align: center;
}

.footer-nav-item-active {
  background: linear-gradient(135deg, rgba(31, 106, 70, 0.12), rgba(199, 141, 63, 0.12));
}

.footer-nav-item-pressed {
  transform: translateY(1rpx);
}

.footer-nav-label {
  font-size: 28rpx;
  font-weight: 700;
  color: #223c2b;
}
