page {
  background-color: #f4efe5;
  color: #223c2b;
}
button {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  background: transparent;
  color: inherit;
  border: none;
  font: inherit;
}
button::after {
  border: none;
}
